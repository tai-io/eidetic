/**
 * SessionStart hook: cross-project memory index injection.
 *
 * Reads native memory files and injects relevant cross-project context
 * into the session start output.
 */

export function run(): void {
  // TODO: Implement in cross-project-index slice
  process.stderr.write('[eidetic] cross-project-index: not yet implemented\n');
}
